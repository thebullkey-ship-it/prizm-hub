services:
  - type: web
    name: prizm-hub-proxy
    runtime: node
    plan: free
    buildCommand: chmod +x scripts/sync-baze.sh && ./scripts/sync-baze.sh
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
